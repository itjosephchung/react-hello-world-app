version: v1.0
name: Pipeline 2
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2004
blocks:
  - name: Rlease
    task:
      jobs:
        - name: gh release
          commands:
            - echo "release job"
      secrets:
        - name: gh-release
